echo "Make sure you opened the http port on the azure dashboard"

sudo cp node-silverscreen.conf /etc/init >> log

sudo cp *.list /etc/apt/sources.list.d >> log

sudo apt-get -u upgrade >> log

sudo apt-get update >> log

sudo apt-get install git >> log

sudo apt-get install -y --force-yes nodejs >> log

sudo apt-get install -y --force-yes npm >> log

sudo apt-get install -y --force-yes make >> log

sudo apt-get install -y --force-yes g++ >> log

sudo npm install -g node-gyp >> log

node –v >> log

sudo apt-get install -y --force-yes redis-server >> log

sudo /etc/init.d/redis-server restart >> log

redis-cli PING >> log

echo "cd to your home directory"

echo "mkdir projects"

echo "cd projects"

echo "git clone your repository."

echo "cd to application directory you just cloned and type npm install"

echo "sudo vi /etc/init/upstart.conf and verify project directory is correct."

echo "type sudo start node-<your upstart name> to start node server"

echo "cat /var/log/node-<your upstart name>.log to verify port is open"

echo "surf to azure url and verify site is up and running"

